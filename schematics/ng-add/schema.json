{
  "$schema": "http://json-schema.org/schema",
  "id": "NgAddSchematicsSchema",
  "title": "Schematics to add ngx-thunder",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
